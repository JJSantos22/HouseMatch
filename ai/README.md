# HouseMatch AI Module

This module contains the deterministic bedroom compatibility scorer used by the backend.

## Public API

The backend integrates through `com.tecstorm.housematch.ai.CompatibilityScorer`.

## Tests

Run the AI module tests from the repository root:

```shell
mvn -pl ai test
```
